import type { Metadata } from 'next';

import TimetableStudioContainer from '@/containers/Tenant/TimetableStudio';

export const metadata: Metadata = {
  title: 'Timetable Studio | Gurukul',
  description: 'Generate, review, and publish master timetable versions.',
};

export default TimetableStudioContainer;
