import type { Metadata } from 'next';

import TeacherConstraintsContainer from '@/containers/Tenant/TimetableSetup/TeacherConstraints';

export const metadata: Metadata = {
  title: 'Constraints · Timetable Setup | Gurukul',
  description: 'Set teacher workload limits and availability.',
};

export default TeacherConstraintsContainer;
