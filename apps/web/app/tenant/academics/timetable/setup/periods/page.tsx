import type { Metadata } from 'next';

import PeriodTemplatesContainer from '@/containers/Tenant/TimetableSetup/PeriodTemplates';

export const metadata: Metadata = {
  title: 'Periods · Timetable Setup | Gurukul',
  description: 'Define the daily period structure and working days.',
};

export default PeriodTemplatesContainer;
