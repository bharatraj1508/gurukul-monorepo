import type { Metadata } from 'next';

import RoomsContainer from '@/containers/Tenant/TimetableSetup/Rooms';

export const metadata: Metadata = {
  title: 'Rooms · Timetable Setup | Gurukul',
  description: 'Manage rooms and specialised spaces for scheduling.',
};

export default RoomsContainer;
