import type { Metadata } from 'next';

import SubstitutionsContainer from '@/containers/Tenant/TimetableSetup/Substitutions';

export const metadata: Metadata = {
  title: 'Substitutions · Timetable Setup | Gurukul',
  description: 'Record covering teachers for specific dates.',
};

export default SubstitutionsContainer;
